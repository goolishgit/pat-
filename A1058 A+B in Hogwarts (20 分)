#include<iostream>

int main()
{
	int a, b, c, d, e, f, i;
	scanf("%d.%d.%d %d.%d.%d", &a, &b, &c, &d, &e, &f);
	a += d;
	b += e;
	c += f;
	f = c / 29;
	c = c % 29;
	b += f;
	e = b / 17;
	b = b % 17;
	a += e;
	printf("%d.%d.%d", a, b, c);
}
